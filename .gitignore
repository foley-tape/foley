# 隐私（M2.6 G7/甲-2 修订）：原始蒸馏带与汇报产物永不入库（含密钥风险）。
# 五卷标准带以**脱敏合成骨架**入库（G7 同一把尺：时间相对化＋非内建工具名加盐哈希＋
# sourceHash=redacted）——clean clone npm test 必须全绿（甲-2）；原始真身在 tapes/raw/，永不入 git。
# 注意规则次序：负号白名单必须排在 tapes/* 与 *.tape.jsonl 之后才生效。
tapes/*
runs/

# 校准数据不入 git（仅下方五卷脱敏骨架＋README 白名单除外）
*.tape.jsonl
!tapes/README.md
!tapes/storm.tape.jsonl
!tapes/smooth.tape.jsonl
!tapes/busy.tape.jsonl
!tapes/jam.tape.jsonl
!tapes/silence.tape.jsonl

node_modules/
*.log
.DS_Store

# 合并立法（2026-07-13·老终端五件套）：渲染产物不入库——源 hero_scene.py＋ref/ 入库即可再生；
# 条帧/样片 ~977MB 属产物（证据副本归 ~/Desktop/至架构师，重件走 Releases 惯例）。
art/hero/renders/

# Releases 打包产物（scripts/pack-records.mjs 出件，上传后即弃——重媒体不入库 §0.5）
dist-records/

# 出厂唱片音频永不入 git（§0.3 不进 tarball＋§0.5 存指纹不存重媒体——SOUND-R4 落仓起彻底执行：
# 盘上文件=vendored 播放位；真身挂 Releases records-v1；指纹在 catalog/manifest/PROVENANCE）
sound/records/*.mp3

# M-T2 62MB 接带已迁 Releases（§B.3）——本地保留、指纹见 docs/records/mt2/stage/RELEASES-MANIFEST.md
docs/records/mt2/stage/*.mp4

# ARCHIVE-1：性格照/定妆照/仪式屏录已迁 Releases media-archive-v1——指纹见各夹 RELEASES-MANIFEST.md
docs/records/m19/性格照/portrait-*.mp4
docs/records/m21/stage/性格照/portrait-*.mp4
docs/records/m22/stage/*.mp4
docs/records/mt1/stage/*.mp4
