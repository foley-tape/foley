# TAPE-0 施工反馈 · M0–M1（施工方 → 架构师，船长转达）

engine `tag m1` (58798a4) / params `8d12782a` / 日期 2026-07-04

## 一句话

§6 引擎数学全部落地，§10 七项金测试全绿，确定性复验逐字节一致。**但用 §6 起手值跑真实带，张力明显偏冷**——地狱带（79 次失败）峰值 T 只有 **0.28**、全程 99% "晴天"；死循环带峰值 **0.08**。根子是规范一个**空白**（失败操作的幅度没定义），不是权重没调对。下面 5 个决定请你回传 `params.json` 或裁示，我据此 replay 复算，**不越 M1 闸门**。

---

## 二、最要紧的一件事：失败操作的"幅度 m"是规范空白 ⚠️

- §4 定义 `m`：READ=内容 KB、WRITE=diff 行、RUN=秒。**但失败的读没有内容** → `m ≈ 0`。
- 后果：两卷带里绝大多数失败是 `READ-FAIL`，`m≈0` 把它们全压平了。地狱带 79 次失败、死循环撞墙 9 次，充能却微乎其微：
  - 地狱带 峰值 T=0.28（S 峰值 0.32），天气 99% CLEAR / 1% OVERCAST，**从没到过 RAIN/STORM**。
  - 死循环带 峰值 T=0.08，全程 CLEAR，针几乎不动。
- **这一条不定，调权重都白搭**——失败读的 m 是 0，乘任何权重还是 0。

**请你定**：失败操作的 m 取什么？
| 选项 | 效果 | 我的看法 |
|---|---|---|
| A 兜底 0.3（无法测量尺寸时用默认值） | 失败读也有基础充能，rep 再放大 | **倾向 A**，最贴合"其余默认 0.3" |
| B 失败即取该动词满幅 m=1 | 失败很重，张力猛涨 | 可能过冲 |
| C 维持现状 m≈0 | 只有 rep 在推，张力冷 | 现状，不推荐 |

---

## 三、与规范的出入（现实修正，逐条）

| # | 规范说 | 现实是 | 我做了 | 要你签？ |
|---|---|---|---|---|
| 1 | §9 一卷带≈一场会话 | 同文件被 resume/续跑跨日追加，墙钟见过 **~11 天** | 加"活跃时长"指标剔空档；回放对 >2min 空档做压缩防爆炸 | 是否要"按 >N min 空档切会话" |
| 2 | §4 ASK 无源工具，靠 >15s 启发式 | 有显式 `AskUserQuestion` 工具（精确得多） | 按冻结的 §4 归 OTHER 计数上报，**未擅改** | 准不准我改 `AskUserQuestion→ASK`（不动协议） |
| 3 | §5 RUN 时长=收发时间戳差 | Bash 结果直接带 `durationMs` | 优先用它，缺才回退时差 | 仅告知 |
| 4 | （未明确）事件落在哪个时刻 | tool_use 与 tool_result 两个时刻 | `moment.t` 取**结果到达时**（outcome 可见时张力才响应） | 认不认这个语义 |
| 5 | §6.3 活跃度是"事件/分钟 EMA" | 未给 EMA 窗口数值 | 取 30s（引擎常量 ACT_WINDOW_SEC） | 可回传覆盖 |
| 6 | §7 单 agent，agent="main" | 日志有 `isSidechain` 可精确认子 agent | v0 全折叠 main，仅计数，留多轨地基 | 仅告知 |

（协议 schema §7 一字未改。）

---

## 四、需要你拍板的 5 个决定（回传 params.json 或一句裁示）

1. **失败操作 m** —— 见【二】，卡点。选 A/B/C。
2. **衰减 τ**（现 60s，断流后 180s）—— 失败成簇散布时 S 在簇间就漏光了、攒不起来。你要"**来得快去得快**"（保持 60s）还是"**攒着劲慢慢消**"（调大到 120–180s）？
3. **RESOLVE 门槛**（现 S>0.3）—— 地狱带 S 峰值才 0.32，`test-OK`/`SAVE` 触发 RESOLVE 的条件几乎踩不到，**全程 RESOLVE×0**。降门槛，还是靠【二】把 S 整体抬高后自然满足？
4. **AskUserQuestion→ASK** —— 准不准我在适配器落地（精确 ASK 优于 15s 启发式，不动协议）。
5. **换 hell 首卷？** —— 现首卷是多日续跑文件（墙钟 11 天、无单一大弧）。次选 `4a0286c7`（308 事件 / 活跃 224min / 更连贯）可能更适合当"张力弧完整"的地狱带。

---

## 五、我做了的更好路径（已落地，供参考）

- **活跃时长指标**：剔除多日续跑空档，smooth 判据落在真实工作时长上。
- **durationMs 优先**：RUN 时长更准，不受尾随乱序影响。
- **回放空档压缩**（IDLE_CAP 2min）：多日文件不再把 20Hz 步进撑爆。
- **sig = 错误首行归一化**（抹路径/数字/hex）后哈希：同一类失败稳定聚类，卡碟探测准（地狱带 STUCK_LOOP×24、死循环×7，探测器本身工作正常）。

---

## 六、关于 §11 校准问卷（我改了，请你认）

- **原问卷的前提不成立**：Q1–Q5 假设"你记得那场会话的情绪"。但带是 `scan` 自动提名的，船长对它**没有记忆**，凭记忆校准这条路走不通。
- **我把它从"像不像你记忆"改成"你想要它成为什么"**（意图版，见每份 REPORT 末尾）。owner 对意图有判断，不需要记得那天。
- **§11 格式我动了两处**（记此备你裁决）：① 每份报告里重复的"现实修正"收敛成一行、指向本文件（原样五条在每份报告里是纯冗余）；② 问卷换成意图版。若你要恢复原样，一句话我改回。

---

## 七、闸门

停在 M1。M2（针＋曲线＋三音＋live 尾随）代码就绪待命，**你的校准签核前不动一行**。
