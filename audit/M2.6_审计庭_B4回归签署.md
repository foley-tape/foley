# M2.6 · 审计庭 B4 回归签署「雷已排」（合龙令 己-0）

> **验证方**：审计庭（独立第三方新鲜会话）——非 RECON（B4 原发现者）、非轨丙（修复者）。三方独立。
> **奉令**：第四号手令 **己-0**「审计庭：于 `track/c-security` 复跑 `audit/repro/b4_probe.mjs` → 签 B4『雷已排』」。
> **被验对象**：轨丙 **B4**（`c69d53a` serve `/records/**`＋`/sound/assets/**` factory 回退＋三闸）；
> RECON 新雷（乙-② 归轨丙）——打包态出厂唱片/床音永 404。
> **被验分支**：`track/c-security = 7b42839`（B4=`c69d53a`／canonical 兵器库=`596e19f`／FEEDBACK=`7b42839`）。
> **复核环境**：worktree `/Users/shadow/tape0-c-security`（软链主检出 node_modules；工作树天然复现打包态——
> `sound/records/*.mp3` gitignored 不在树，故 factory 回退路径真被触达）。
> **日期**：2026-07-07。

---

## 结论：✅ B4 雷已排 —— **审计庭签「雷已排」，授权 己-1 合入 `track/c-security`**

`serve` 打包态出厂音频粮道（factory 回退）＋安全三闸（只读／fence 前缀／文件名白名单）经审计庭
**四路复核全过、零泄漏、零绕过**；且 B4 对 `serve.mjs` 的 49 行改动**未回潮 F1/F5**。

## 一、四路证据（`track/c-security = 7b42839`）

| 路 | 手段 | 结果 |
|---|---|---|
| **[A] 丙自带复现** | `node audit/repro/b4_probe.mjs`（6 向量） | **6 pass / 0 fail**（exit 0）：`saturation.mp3`→200 factory 真字节★／`evil.mp3`→404 白名单外／`still-life.mp3`→404 诚实缺／`../../SECRET`＋编码穿越→404／`catalog.json`→200 |
| **[B] 回归金测试** | `node --test golden/b4.factory.test.ts` | **7 / 7 pass**（factory 回退＋投毒拒＋诚实 404＋三闸穿越＋元数据不误伤＋源码卫兵） |
| **[C] 邻雷回归** | `bash audit/repro/M2.6_regress_serve.sh`（F1+F5 于**丙改后的** serve.mjs） | **rc=0**：`/%zz`·`/%`→400 进程存活；坏 Host 读写同门→403、合法→200——**B4 改 serve.mjs 未回潮 DoS/Host 闸** |
| **[D] 审计庭独立红队补充** | 自备 18 对抗向量（穿越多编码／白名单绕过／跨面拐取／多深度埋密） | **18 pass / 0 fail / 0 泄漏**（exit 0） |

## 二、[D] 红队补充详情（审计庭自备，非只跑 happy-path）

B4 系**安全敏感的路径穿越面**（003 令乙-②裁「必须住安全大脑」），且 [A] 探针系**修复者自撰**——
故审计庭另备对抗向量独立复射，专打穿越/白名单/泄漏三面：

- **穿越（全灭·非 2xx 且不泄）**：全 %-编码 `%2e%2e%2f`×3 深、双重编码 `..%252f`、点点滑动 `....//`、
  反斜杠 `..%5c`、空字节 `saturation.mp3%00.txt`、资产面 `/sound/assets/..%2f..%2f..%2f`、
  跨面拐取 `/sound/assets/../records/factory/evil.mp3`、段拼接 `/records/saturation.mp3/../evil.mp3`。
- **白名单绕过（evil 必 404）**：`evil.mp3`／`evil.mp3?x=saturation.mp3`（query 走私）／`EVIL.MP3`（大小写）／
  `evil.mp3.`（尾点）／`/sound/assets/evil.wav`——**均 404**，catalog 未在册即使落 factory 也一律不供。
- **多深度埋密**：`$HOME/{,.foley/,.foley/records/}SECRET_{3,2,1}UP.txt` 三层诱饵，**任一穿越向量均未取到**
  （响应体零 `LEAK-*` 标记）。
- **正向控制**：`saturation.mp3`／`warm-fuzz.mp3`（repo 缺）→200 **factory 真字节**；
  `l1-roomtone.wav`（repo 真身在树）→200 **RIFF**——证 **repo 真身→factory** 解析优先级正确（真身命中不误走 factory）。

## 三、关键审计判断

1. **「改动之后必复射」**（承戊之独立价值判断）：B4 动了 `serve.mjs`（+49/-1）；审计庭据此对 **F1/F5** 于
   丙改后的 serve 复射（[C]），确认安全邻雷未被 B4 顺带打破——非只验 B4 自身。
2. **不采信修复者自证**：[A] 之外自备 [D] 18 向量，红蓝分离落到向量层。穿越/白名单/泄漏三面在**审计庭自撰
   攻击**下无破。
3. **coverage 诚实记账**：assets/wav happy-path 因工作树 wav 系 tracked 真身、无法在本树触达 factory 分支
   （金测试 #5 源码卫兵＋打包 E2E 另证）；审计庭 [D] 已独立复核 **assets 面的穿越闸**（`/sound/assets/..` →404），
   安全面无盲区。

## 四、签署

**【B4 · 审计庭签「雷已排」】** —— 验于 `track/c-security = 7b42839`，2026-07-07。
- 覆盖：打包态 factory 回退（records 实触达＋assets 穿越闸）＋三闸（只读/fence/白名单）＋不回潮 F1/F5。
- **授权合龙令 己-1**：可合 `track/c-security`（`c69d53a→39aaed9→596e19f` --no-ff）。GATE 翻绿归主检出操作员（戊 授权），审计庭不动 `GATE.md`。

## 五、复算命令（架构师可原样重跑）

```bash
WT=/Users/shadow/tape0-c-security               # 或干净检出 track/c-security @ 7b42839
[ -e "$WT/node_modules" ] || ln -s /Users/shadow/tape0/node_modules "$WT/node_modules"
node "$WT/audit/repro/b4_probe.mjs"                       # 期 6 pass / 0 fail
( cd "$WT" && node --test golden/b4.factory.test.ts )     # 期 7/7
bash "$WT/audit/repro/M2.6_regress_serve.sh"             # 期 rc=0（F1+F5 未回潮）
# 审计庭红队补充脚本随签附（scratchpad b4_redteam_supplement.mjs，18 向量/0 泄漏）
```

## 六、边界

只读源码；**未改轨丙 worktree**（红队补充脚本落 scratchpad，未入丙树）；未提交；未碰 `GATE.md`（翻绿归操作员）、
未碰 `stage/fixtures/captain.*`（船长 B8）。开工查并发：无会话 cwd 在 c-security、audit/ 20 分钟内无写入。

（审计庭 B4 复核完毕；三方独立恪守——非发现者非修复者、修复者探针之外自备对抗向量、逐项经验证据。B4 雷已排。）
