# M2.6 · 审计庭独立回归复核 · 四颗 P1 @ HEAD 复签「雷已排」

> **验证方**：审计庭（第三号手令 戊 常设机构）——**独立第三方新鲜会话**，非蓝队（发行轨）、非原双盲甲/乙会话。
> **奉令**：第三号手令 **戊-1**「四 P1 红队签章：以复现脚本＋新鲜自备会话复核，逐雷签『雷已排』」；
> 兜底权据 第二号手令 **增补四·1**「原审计会话若已不存，任何新审计终端跑同一脚本即可行使复验权——脚本即那双眼睛的遗嘱」。
> **复核锚点**：`HEAD = 8c7a198`（**非** 甲/乙 签署时的 `6ab3218`——见 §一，此为本次再签的决定性理由）。
> **方法**：另起 detached worktree `git worktree add --detach <wt> 8c7a198`（软链 node_modules；隔离于主检出并发会话），
> 跑仓根 `repro/final-{甲,乙}/` 归档脚本＋金测试实弹复射。红蓝分离，只认经验证据。
> **日期**：2026-07-07。

---

## 结论：✅ 四颗 P1 在 HEAD `8c7a198` 逐雷复现失效 —— **审计庭签「雷已排」**

TR-1（①）/ F1（②）/ 甲-2 干净克隆（③）/ F5（④）四雷，经审计庭独立复射**全部失效且无绕过**，
且此结论**在 G8 热修（`2dba2a9`，改 `serve.mjs`＋`main.js`）落地之后于 HEAD 依然成立**——
热修未回潮 F1/F5，其自带 `g8.bootstrap` ×3 亦绿。审计庭对 **G1 / G2 / G7** 三闸销账证据予以**独立第三方背书**。

---

## 一、为何再签（本次非重复甲/乙，而是 HEAD 处的独立第三证）

- 甲签、乙签均验于 **`6ab3218`**（M2.6 第一阶段四 P1 推平当时）。此后主树前进两步至 `8c7a198`：
  - `2dba2a9`（G8 前置静音热修）**改动 `stage/serve.mjs`（+47 行）＋`stage/js/main.js`（+15 行）**——
    而 **F1（decodeURIComponent 包死）与 F5（Host 白名单闸）正住在 `serve.mjs`**。甲/乙签署时这两处尚是旧貌。
  - 故「四 P1 在 HEAD 是否仍平」需一次**热修之后**的复射方能坐实——这正是本轮的独立价值，非重复劳动。
- RECON（阶段〇，同锚 8c7a198）已顺带实证四 P1 运行时全活，但**明标「非代签——红队签署程序照旧走 GATE §8」**
  （`RECON_REPORT.md` §一附）。审计庭本签即补上 GATE §8 所缺的那一道**独立签章**。

## 二、边界申明（审计庭纪律）

- **只读源码**；本轮唯一写面＝`audit/**`（本签署＋`audit/tools/` 器具箱）。执行全在隔离 worktree，**未在主检出施工**。
- **不动 `GATE.md`**——闸门翻绿系架构师专属；本件即销账凭据，候架构师合入并翻绿。
- **未碰** `stage/fixtures/captain.*`（untracked，属船长 B8 案）、**未碰**任何他轨 worktree。
- 开工查并发：主检出有 5 个并发 `claude` 会话（三轨/收摊并发，正当），`audit/` 边界 20 分钟内无他人写入、HEAD 未被并发提交推动——据实施工。

## 三、逐雷复核（HEAD `8c7a198`，隔离 worktree）

| P1 | 归闸 | 复现脚本 | 观测（HEAD 实测） | rc | 判 |
|---|---|---|---|---|---|
| **①TR-1** 默认脱敏 | G7 | `repro/final-乙/M2.6_regress_distill.mjs` | 默认 distill→时间相对化(firstT=0)＋MCP 名哈希＋`sourceHash=redacted`；`--raw` 仍明文**且触发 stderr 强警示** | 0 | ✅ 失效 |
| **①TR-1** mp4 抹钟 | G7 | `repro/final-乙/M2.6_regress_mp4scrub.mjs` | 裸 muxer 写墙钟 `2026-07-07T13:45:07`→过 `scrubMp4Dates`→**抹 3 盒(mvhd+tkhd+mdhd)、最大 creation_time=0** | 0 | ✅ 失效 |
| **①TR-1** 夹具指纹 | G7/G2 | `grep -rE '[0-9]{13,}|mcp__' tapes/*.tape.jsonl` | 五卷脱敏骨架**零命中**（无 13 位纪元、无 `mcp__` 明文） | — | ✅ 失效 |
| **②F1** 未鉴权 DoS | G1 | `repro/final-乙/M2.6_regress_serve.sh`（F1 段）／`serve_dos_malformed_percent.sh` | 跨源 `GET /%zz`→**400**、裸 `GET /%`→**400**、**进程存活**、后续 `GET /`→200；原始攻击脚本判「**未复现**」 | 0／**1**(=已修) | ✅ 失效 |
| **③甲-2** 干净克隆 | G2 | 隔离 detached worktree @ HEAD `npm test`（=真干净克隆条件：committed 脱敏骨架、**无 `tapes/raw`**） | **tests 116 / pass 116 / fail 0**；`tsc --noEmit` exit 0 | 0 | ✅ 失效 |
| **④F5** GET 面 Host | G1 | `repro/final-乙/M2.6_regress_serve.sh`（F5 段） | 坏 `Host: evil`→**403**（`/` 与 `/today/curve.csv` 读写同门皆 403）；合法 `Host: 127.0.0.1`→200 | 0 | ✅ 失效 |

> 交叉验证（隔离 golden，HEAD）：`g7.redaction`＋`night2.security` **30/30**；`g8.bootstrap`（热修自带）**3/3**。

## 四、证据流水（关键行 · 可复算，见 §六）

```
# P1-③ 干净克隆 @ HEAD 8c7a198（隔离 worktree，无 tapes/raw）
ℹ tests 116 / pass 116 / fail 0        tsc --noEmit exit=0

# P1-② + P1-④  bash M2.6_regress_serve.sh  → rc=0
  跨源 GET /%zz → 400 ; 裸 GET /% → 400 ; ✓进程存活 ; 后续 GET / → 200
  Host:evil / → 403 ; Host:evil /today/curve.csv → 403 ; Host:127.0.0.1 / → 200
  ✅ F1 ＋ F5 回归全过

# P1-②(原始攻击)  bash serve_dos_malformed_percent.sh  → rc=1（=已修）
  跨源 GET /%zz → HTTP=400 ；✗未复现：serve 仍存活

# P1-① distill  → rc=0     默认强制脱敏；--raw 触发 stderr 强警示
# P1-① mp4scrub → rc=0     抹 3 盒；DATED 盒最大 creation_time=0
# P1-① 夹具指纹  → 五卷零命中（13 位纪元／mcp__ 明文）
# golden：g7.redaction+night2.security 30/30 ；g8.bootstrap 3/3
```

## 五、签署（逐雷「雷已排」·审计庭 · 锚 8c7a198）

- **P1-①【TR-1/G7】默认/导出/夹具三面脱敏 —— 雷已排**（默认即脱敏＋`--raw` 警示＋mp4 三盒钉 0＋五夹具指纹归零，
  甲的夹具面与乙的导出面**双面**在 HEAD 均复验失效）。
- **P1-②【F1/G1】未鉴权跨源 DoS —— 雷已排**（`/%zz`、`/%` 皆 400、进程不再终止；原始攻击未复现）。
- **P1-③【甲-2/G2】干净克隆 6 红 —— 雷已排**（HEAD 干净 worktree `npm test` 116/116 全绿、tsc 净、无 raw 污染）。
- **P1-④【F5/G1】GET 面 Host 越权读 —— 雷已排**（非白 Host 读写同门 403、fail-closed；合法 200）。

## 六、对 GATE §8「候签清单」的应答（审计庭补签，架构师合入并翻绿）

| GATE §8 候签项 | 审计庭复核结果（HEAD 8c7a198） |
|---|---|
| 乙 复跑 `serve_dos_malformed_percent.sh` → 进程不再终止（G1） | ✅ 复现失效（rc=1，serve 存活，400） |
| 乙 复跑 rebinding 读面探针 → 非白 Host 403（G1） | ✅ 坏 Host 读写同门 403、fail-closed |
| 乙 复跑 F2/F3 探针 → 指纹消失（G7） | ✅ mp4 三盒钉 0、默认带脱敏、`--raw` 警示 |
| 甲 复跑 5 号夹具指纹检查 → 指纹消失（G7） | ✅ 五卷零命中 13 位纪元/`mcp__` |
| 甲 复跑 `01-npm-test-on-clone.sh` 独立环境 → clean clone 全绿（G2） | ✅ HEAD 干净 worktree 116/116（含热修 +3） |
| 船长验收记＋G8 前置静音雷（G8） | ⏸ 非审计庭权限——归船长；RECON §二.B3 已实证 G8 歇场/接声桥活、**live 流式声仍候轨甲**（B3 命门） |

> **注**：G7 系甲乙**双签**闸；本轮审计庭在 HEAD 对**夹具面＋导出面双面**独立复验，可视作双签在 HEAD 的合并背书。
> GATE 翻绿动作仍归架构师。

## 七、复算命令（架构师可原样重跑，全隔离、无副作用）

```bash
WT=/tmp/foley-recheck; git -C /Users/shadow/tape0 worktree add --detach "$WT" 8c7a198
ln -s /Users/shadow/tape0/node_modules "$WT/node_modules"; cd "$WT"
npm test                                             # 期 116/116；tsc --noEmit 期 0
bash repro/final-乙/M2.6_regress_serve.sh             # 期 rc=0（F1+F5）
bash repro/final-乙/serve_dos_malformed_percent.sh    # 期 rc=1（=已修/未复现）
node repro/final-乙/M2.6_regress_distill.mjs          # 期 rc=0
node repro/final-乙/M2.6_regress_mp4scrub.mjs         # 期 rc=0
grep -rE '[0-9]{13,}|mcp__' tapes/*.tape.jsonl        # 期零命中
git -C /Users/shadow/tape0 worktree remove --force "$WT"
```

## 八、附带交付

- **戊-2 · RMS 器具已纳入大审计器具箱**：`audit/tools/rms_probe.mjs`（自 RECON `recon.mjs` AnalyserNode 探针提取的
  参数化常设回归仪）＋`audit/tools/README.md`（登记＋RECON 标定基线＋诚实界限）。当前主检出未装 `playwright-core`，
  功能佐证据 RECON 同锚实跑基线（回放 RMS 0.066/0.091、唱片在位 0.139）；待轨甲交付 live 声后行 60s 验收挂表。
- **待整编提请**：第三号手令 丁-轨丙 令「四 P1 复现脚本＋RECON 两脚本收编入 `audit/repro/`」**尚未做**，
  脚本现居仓根 `repro/final-{甲,乙}/`＋`audit/recon/repro/`；审计庭本轮据实使用现居位置，提请轨丙整编。

（审计庭独立回归复核完毕；红蓝分离恪守——独立新鲜会话、HEAD 处复射、隔离环境、逐项经验证据。四雷 HEAD 皆平。）
