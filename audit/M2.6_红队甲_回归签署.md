# M2.6 第二阶段 · 红队甲独立回归验证 · 合规签署

> 验证方：红队甲（原 §1 文档＋§3 视觉审计会话）｜红蓝分离，不采信发行轨声明，只认经验证据。
> 被验对象：发行轨（蓝队）`origin/main = 6ab3218`「四颗 P1 物理推平」之 **P1-③（干净克隆 6 红）** 与 **TR-1 夹具隐私留痕**。
> 环境：工作树 `tape0-final-jia` 已 `git reset --hard origin/main`（HEAD=6ab3218）；干净复测另起全新临时工作树，杜绝本地 `tapes/raw/` 污染。
> 日期：2026-07-07。

---

## 结论：✅ 双项过硬 —— **签署【G2 待补 销账 · 绿灯投掷】**

P1-③ 与 TR-1（夹具指纹）经外科手术式复核**全部达标**：干净检出 **113/113 全绿**、tsc 净、五卷脱敏骨架**指纹绝对归零**。红队甲对 **G2「补脱敏合成夹具/clean-clone 全绿」** 销账**投绿灯**。

---

## 1. 环境对齐（如指令）

```
$ cd /Users/shadow/tape0-final-jia
$ git fetch origin && git reset --hard origin/main
HEAD is now at 6ab3218 M2.6 第一阶段：四颗 P1 物理推平…
```
- origin/main 领先旧锚 149ddea 两提交：`a515a5b`（终审板＋GATE 补丁入库·架构师签核）、`6ab3218`（四颗 P1 推平）。
- 对齐后 `tapes/` 已被 committed 脱敏骨架覆盖（我审计期拷入的 5 卷原始污染拷贝被 reset 清除）；**无 `tapes/raw/`**；`.gitignore` 白名单次序正确（`tapes/*` 后接 `!tapes/{五卷}.tape.jsonl`＋`!README.md`，仅此 6 件入库）。

## 2. Part A · 干净检出复测（P1-③ / G2）

**方法**：另起全新临时工作树 `git worktree add --detach <tmp> origin/main`（零本地状态、**无 `tapes/raw/` 缓存**），仅软链 node_modules，直跑 `npm test`。

```
HEAD: 6ab3218
committed 标准带: busy jam silence smooth storm（.tape.jsonl）
raw 缓存存在? ✓无(真干净)

ℹ tests 113
ℹ pass  113
ℹ fail  0
tsc --noEmit exit=0
```

**判读**：真干净检出（无任何原始带缓存）下 **113/113 全绿、零失败、tsc 净**。较旧锚 105 测试 +8 回归（g7 脱敏×5、F1 崩溃×2、F5 Host×1）。P1-③「干净克隆 6 红」**已物理推平**——脱敏合成骨架足以令 clean clone 全绿，且这正是公开镜像（orphan-clean）会得到的环境。**过硬。**

## 3. Part B · 夹具静态指纹逆向扫描（TR-1 夹具面）

对五卷 committed 脱敏骨架穷尽检索：

| 指纹类 | 原始真身（审计期实见） | committed 脱敏骨架 | 判 |
|---|---|---|---|
| 绝对 epoch（13 位 ms） | `t:1782527212340`、`firstT:1782527203147` | **零 13 位以上数字**；`firstT:0`、时间相对化（最大 8 位相对 ms） | ✓ 归零 |
| `mcp__` 明文标识 | 明文 MCP/自定义工具名 | **零 `mcp__`**；自定义名哈希为 `t<hash>`（t34b95a03…） | ✓ 归零 |
| errClass 自由文本 | 主机名/自由错误串（乙-F4/F6 靶） | 全 `e<hash>` 加盐，**无明文** | ✓ 归零 |
| sourceHash 文件指纹 | fnv 十六进制（如 c8592479） | 五卷全 `"redacted"` | ✓ 归零 |
| sig / targetHash | `sig:7e3198ac`、`targetHash:bee4f76d` | `sc689183d` / `a7a91e09`——**原始值零残留**（加盐重写） | ✓ 归零 |
| 邮箱 / 绝对路径 / token | — | **零命中** | ✓ 归零 |
| distiller 标记 | `distill/2` | `distill/2+redact`（脱敏形态自证） | ✓ |

**透明记账（我检过并判为合规，非遗漏）**：唯一保留的明文是 **Claude Code 内置工具名**（Bash/Read/Edit/Write/WebFetch/WebSearch/Agent/AskUserQuestion/ToolSearch）。此类名对**全体 Claude Code 用户通用**，不泄仓库/组织/用户身份，非身份指纹；且金测试需其类型信息。**敏感的自定义/MCP 名已哈希**。据「13 位 epoch／mcp__ 明文／敏感指纹绝对归零」判据——**达标**。

## 4. 签署

**【G2 待补 销账 · 绿灯投掷】** —— 红队甲，2026-07-07，验于 `origin/main = 6ab3218`。

- 覆盖范围：**G2**（补脱敏合成夹具 → clean-clone 113/113 全绿）＋ **TR-1 的夹具指纹面**（五卷绝对归零）。
- **不在本签范围**（据发行轨「候签清单」）：**G1**（乙-F1 serve DoS 修复）候乙签；**G7 导出脱敏**（mp4scrub/默认 redact/dub meta 抹钟）为双签——本次 113/113 含 `g7.redaction.test.ts ×5` 全绿，**旁证** G7 机制在位，但其终签属甲乙双签，非甲独closeable。
- 复算命令（架构师可原样重跑）：
  ```
  git -C /Users/shadow/tape0 worktree add --detach <tmp> origin/main
  ln -s /Users/shadow/tape0/node_modules <tmp>/node_modules
  cd <tmp> && npm test            # 期 113/113
  grep -rE '[0-9]{13,}|mcp__' <tmp>/tapes/*.tape.jsonl   # 期零命中
  ```

（红队甲回归验证完；红蓝分离铁律恪守——独立环境、独立复算、透明记账、逐项经验证据。）
