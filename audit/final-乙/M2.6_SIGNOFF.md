# M2.6 · 红队乙独立回归背书（G1 乙签 / G7 乙半签）

**验证者**：红队乙（隐私与安全靶区，§2）
**对齐快照**：`main = 6ab32186e8da1c0df423eb72c5b539904559ac6b`（M2.6 第一阶段四颗 P1 推平）
**方法**：非破坏性 rebase 我的审计分支到 6ab3218（保住原攻击脚本），用**我自己的**探针实弹复射，再与发行轨金测试交叉验证。**未看甲**（双盲对甲维持；甲专属项 P1-③ 不在我签署范围）。

---

## 一、逐向量裁决（全部失效 = 已修）

| 原发现 | 对应 P1 | 修复位 | 我的独立复射 | 裁决 |
|--------|---------|--------|--------------|------|
| **F1** serve DoS（畸形 %-崩进程） | P1-② | `serve.mjs:251` decodeURIComponent 包 try→400；`:104` new URL 同包；`:273` 进程级 unhandledRejection 兜底 | `/%zz`＋裸 `/%`（带跨源 Origin）→ **400**，进程**存活**，后续 `GET /`→200 | ✅ 失效 |
| **F5** GET 面零 Host 校验（DNS-rebind 读） | P1-④ | `serve.mjs:102` HOST_OK 白名单置路由最前，读写同门 | `Host: evil`→**403**（`/` 与 `/today/*.csv` 皆是）；缺/大写/尾点 Host 全 **403**（fail-closed）；合法 Host→200 | ✅ 失效 |
| **F2** 导出 mp4 内嵌墙钟 creation_time | P1-①(隐私真雷) | 新 `stage/js/mp4scrub.js`，`film.js:509` finalize 后挂钩 | 裸 muxer 输出 creation=`2026-07-07T13:45:07`→ 过 `scrubMp4Dates` 后 **3 盒钉 0**，最大 creation=**0** | ✅ 失效 |
| **F3/TR-1** 默认带非脱敏可分享 | P1-①(隐私真雷) | `writeDistilled` 默认 `redact=true`；`--raw` 显式＋stderr 强警示；`dub.js` meta 去 createdAt/liveEpoch | 默认 distill → 时间相对化(firstT=0)＋MCP 名哈希＋sourceHash=redacted；`--raw` 仍产明文且被警示（逃生门语义正确） | ✅ 失效 |

## 二、完备性 / 无绕过核验（红队姿态，非只跑 happy-path）

- **F1**：处理器内**仅有**的两处 per-request 同步抛（decodeURIComponent、new URL）皆已包死返 4xx；POST 体 JSON.parse 本在各自 try 内；另有进程级 unhandledRejection 网做纵深兜底。**未见其他未护同步抛**。
- **F5**：`HOST_OK.has(String(req.headers.host ?? ''))` 缺省拒——缺 Host/大写 LOCALHOST/尾点 Host 实测全 403（fail-closed）；rebind 页 Host=攻击者域→403；直连 127.0.0.1 的 Host 合法→交给 F1 闸接管。两闸各守其面，无缝。
- **F2**：`scrubMp4Dates` 递归 moov/trak/mdia 抹全部 mvhd/tkhd/mdhd 的 creation+modification（v0/v1 两版式）；webm 经查不写墙钟、gif 无日期域——三容器全覆盖。坏盒/截断不越界（发行轨金测试另证）。
- **F3**：默认即 redact（其正确性由既有 `privacy.redteam.test.ts` 七类密钥/MCP/绝对时间断言背书）；`dub.js` 残留的 `liveEpoch` 仅内存消费（直流轴折回），**不落盘 meta**。

## 三、交叉验证
- 我的独立探针：`repro/final-乙/M2.6_regress_{serve.sh,distill.mjs,mp4scrub.mjs}` 全绿。
- 原攻击脚本 `serve_dos_malformed_percent.sh` 现「未复现」（`/%zz`→400、serve 存活）。
- 发行轨金测试 `golden/{g7.redaction,night2.security}.test.ts`：**30/30 通过**（含其 F1/F5 断言）。

---

## 四、签署

> **【G1 · 乙签 ✅ 绿灯投掷】** — §2 隐私与安全靶区：F1(P1-②)/F5(P1-④)/F2·F3(P1-①/TR-1) 四向量经乙独立实弹复射，**全部失效且修复完备无绕过**。乙予以最终安全背书。

> **【G7 · 乙半签 ✅】** — G7 为双签闸；乙就**脱敏/DoS/Host** 诸项签绿。**尚待甲半签**（含甲专属 P1-③「干净克隆 6 红」，我维持双盲未验），G7 待甲签齐方全绿。

**乙不越权改 GATE.md**（只读审计纪律；GATE 销账归架构师）。本背书即销账凭据。

乙 · 锚 6ab3218 · 分支 audit/final-yi
