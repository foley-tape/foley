# TAPE-0 封版前红队 · 交付导读（NIGHT-1）

**这是什么**：两份**互相独立**的盲审红队报告（各自先写盲答再开卷，互不通气），审计对象 `tape0` @ `72e171c`（分支 `audit/night1`）。基线 `npm test` 26/26 绿、`tsc` 干净、`git` 干净、哈希链 params `886928d1` / verdict `verdict/2` 均核验一致。**只读审计**：除 `audit/` 外零改动。

**一句话结论**：**无 P0（不阻断封版）**。引擎数学/确定性/鲁棒性被专门攻击后大面积守住。但**两审独立收敛在 4 条 P1 级问题**——收敛本身是强信号。

## 合并 P1 清单（两审交叉验证后）
1. **诚实条款被破**：分类正则 `\btest\b` / `git\s+commit` 匹配命令**全串** → 无辜命令（`grep "test"`、`echo "…git commit…"`、`curl …/test`）伪造 RESOLVE 和弦并乘法泄真实张力。当前 5 带因触发点 S<0.3 未发作（是运气非设计）。
2. **"零明文分享带"被证伪**：`--redact` 后仍泄——含字母短密钥穿透 errClass 净化（5/7）＋ MCP 工具名原样穿透 ＋ 绝对时间戳 ＋ 保留的 `sig`(32位FNV) 可字典反演。
3. **storm 卡碟判据可信度**：充能侧 `sig` 不含 targetHash → 78 读失败横跨 **61 个不同 URL 塌缩成 18 个 sig**，封版 storm 的"9 卡碟边沿"这条 active 判据实为过度归一产物，并反噬 `resolveOnOpportunity` 退役的"迷路型"论证。
（第 4 条 P1/P2 边界：5 个死参数 + 正则硬编码与 params.json 发散，违"唯一事实源"，且**挡住第 1 条的显然修法**。）

## 两份报告怎么读
- `1_AUDIT_REPORT_A.md`（审计A）：卷尾**附录含与 B 的逐条交叉核对**——直接给出趋同项、各自独有项、合并 P1。**先读这份的执行摘要 + 交叉核对附录**即可拿到全局。
- `2_AUDIT_REPORT_B.md`（审计B）：B 的独立十大发现全文（B-3 卡碟塌缩、B-5 滴灌失效、B-10 针无上夹等细节在此）。
- 两份**互补不冲突**，请读并集。各自都含"分歧席"（盲答 vs 现设计）与"质疑架构师席"。

## 通宵耐力测试
- `3_SOAK_REPORT_A.md`：**6h 真实节奏跑跑满**（RSS 恒 80MB/无泄漏、发射漂移全 0、过期对齐 26ms 不退化）。
- `4_SOAK_REPORT_B.md`：B 的耐力测试。

## 未随包（在 `audit/` 里，需要再取）
- 复现脚本 `audit/repro/*.ts` + `audit/b/repro/*.ts`（+ `.out.txt` 证据捕获）：任一发现存疑时 `node <脚本>` 即可复跑。
- 盲答原文 `audit/PREREG_DESIGN.md` ×2：分歧席已在报告里蒸馏，原文仅备查。
- 原始日志/CSV（soak 滚动日志、listen.raw）：可由 `node audit/soak/soak-summarize.ts <log>` 随时重出报告，无需上传。

## 建议裁决动线
裁 P1（尤①②触宪法/安全两条正典）→ 出修复令 → 修完重跑 26 金测试 + sweep + 两审 repro → 与盲听结论合流 → 封版三件套照旧。
