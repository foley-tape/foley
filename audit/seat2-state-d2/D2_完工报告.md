# D2 修复批次 · 完工报告（席二继任）

交付：席二继任终端 · 2026-07-15 ｜ 收：席一（对审复测签章）／主支架构师／船长案头
分支：`seat/state-d2`（基线 `3fd00b1` 席一工单1/2 之上）｜ 定性：**复审二轮：席一 7 发现全整改·验收全绿·候席一复测签**（一轮 `2069b57` 席一驳回——集成假绿 7 处；本轮为整改，见 §八）
自审纪律：呈上即背书（写实修法令 §三精神下沉至逻辑域）——本报告所述绿灯皆有非空过证据，已知不足如实列于 §六。

## 一、mandate 与做法
D2 修复令：从 `3fd00b1` 新建干净修复分支，**不整体合入** `seat/state@88b4558`、不带 REC 品味刀（b9b1428）与旧交棒文书；选择性移植 `3186e6c/966f3af/4b1635b/48a27dc` 的有效实现，处理与席一在 `connect.ts/serve.mjs/cards.test.ts/instruments.js` 的重叠；六必修＋真验收。

**根因一句话**：单一事实源只做了一半——`deriveMachineState` 纯函数金测全绿（穷举 1536 零非法帧），但运行时被**引擎包旁路**覆写（`Lamps.onPacket` 每收一包用原始 `pkt.pendingAsk` 写琥珀），且验收器**空过**（杀前从未证 REC 真亮）。函数对、集成错、验收假——三层一起才是真绿。

## 二、六必修·缺陷→修（皆有出处）
| # | 真缺陷（实证出处） | 修 |
|---|---|---|
| 1 producer 空过 | `producer_probe` PASS 只断言 `after.rec===false`，从不断言 `before.rec===true`；且点击 (640,60)=#deck 命中区（坑册#2），REC 从未亮。**更深**：无工具纯文本 claude 不产活动包→`link` 恒 `connecting`→REC 永不亮（本轮实证） | 手势 elementFromPoint 验真空白；真工具任务（tool_use→活动包→link=live→REC 真亮）；等待条件律等 REC 亮再杀；PASS 加 `before.producer==='alive' ∧ rec===true` |
| 2 R2 formula | `derive.js:22` `(!live‖link==='live')` 放行非 live 问询；`producer!=='dead'` 放行 `ended` 问询；琥珀可先于红 | 契约 v1.3 先改文＋`asking = recording ∧ pendingAsk`（live 恒 PLAYING 故对真机成立·清零非 live/ended/未录） |
| 3 旁路改写 | `instruments.js:446` 包→pendingAsk（amber）＋包 phase→emerald；`main.js:163` `stopSource` 裸删 `dataset.signal`；`:156` 裸写 `linkUp` | Lamps 退纯渲染器（onPacket 只留 actTarget 魔眼活动强度）；amber=`lamps.pendingAsk`(d.asking)、emerald=`lamps.settled`(d.settled)；signal/linkUp/settled 全经 `refreshMachineState` |
| 4 ask_probe | `line 43` 只设 FOLEY_PROJECTS 不设 FOLEY_HOME→泄漏真 ~/.foley；sustain 只看标志位 | FOLEY_HOME=tmp 隔离；真 sustain=琥珀 tube --lit 反复呼吸（litPeaks≥2·治"只闪一下"）＋窗内 recording=true（琥珀⟹recording·非空过） |
| 5 迁移＋settled/transportRun | **生产侧假绿铁证**：`connect.ts` 只装 `hooks.SessionEnd`，`hook.ts` 有 SessionStart 分支但**从没被安装**→真 `foley connect` 永不注册 PID 心跳→producer 恒 null→REC 撒谎；探针 `--settings` 双注入掩盖之。settled 契约列而 derive 未实装 | `wireSettings` 装 SessionEnd＋SessionStart 双钩子；`wiredIn` 判据改「两钩子俱在才算齐」（旧装再 connect 一次幂等补齐）；settled 入 derive（`done` 输入·R5 power 门）；transportRun 见 §六 |
| 6 假绿陈述 | 旧完工报告/账本称"归一/非空过"与实现不符（derive 绿≠运行时·探针空过） | 不迁旧完工报告；本报告＋账本据实记（诚实不足清单在册） |

## 三、重叠 reconciliation（选择性移植·未整体合入）
- **serve.mjs**（席一 ±196 多为 bundle-slim × 我 +96 producer registry）：逐锚手放九处——import 加 `realpathSync/execSync`（席一 renameSync/rmSync 原样并存）、onSpoolLine 分流 session-start/ended、startCardDuty prime、startLive stderr 锚定、transport `producer` 字段、select/eject arm/disarm、producer registry 全块、`GET /transport`。**行号漂移故手放非 patch**。
- **connect.ts**（席一 ±4 只碰 printTerms × 我 wireSettings/wiredIn）：无交集，wireSettings 双事件版直落；printTerms/console 文案随更。
- **instruments.js**（席一唯一改 `:114 paper.png→webp` 属 ChartRecorder × 我改 Lamps）：无交集，`paper.webp` 保。
- **cards.test.ts**：席一改·我不碰——全量金测证不回归。
- 干净移植（席一未碰·带修非 verbatim）：hook.ts / derive.js / main.js / css / 金测 / 探针。

## 四、验收（真·非空过·D4 全档）
- **定向＋全量金测 192/192·tsc 净**（基线 `3fd00b1` 为 180/0）。新增：derive.test.ts（穷举 3072·asking⟹recording＋settled 全域执法）、producer.test.ts（PROD 五案＋derive 非空过链接）、connect-migration.test.ts（MIG 四案·迁移锁）、live-sound LIVE-8。
- **真 Claude producer_probe 三案全非空过 PASS**（evidence/producer_probe_all.json）：A1 优雅 SIGTERM→ended（607ms·REC 灭·**无死相**）／A2 猝死 SIGKILL→dead（1210ms≤5s·REC 灭·**死相 dead**）／B 自然→ended 无死相。三案 `before.rec===true`（A1/A2）即 REC 真亮过。
- **ask_probe clean HOME PASS**（evidence/ask_probe.json）：latency 500ms·litPeaks 17（真呼吸非闪）·recDuringAsk=true·duck −5.6dB·清除即撤·零页面错。
- 证据级：金测 L0/L1·探针 L2（真 Chromium＋真 Claude）；**候席一 L2 复测＋签**。

## 五、变更清单
改：cli/connect.ts · cli/hook.ts · stage/serve.mjs · stage/js/main.js · stage/js/instruments.js · stage/css/stage.css · golden/live-sound.test.ts
新：docs/状态契约_模式灯语真值表.md(v1.3) · stage/js/derive.js · golden/{derive,producer,connect-migration}.test.ts · stage/tools/verify/{producer_probe,ask_probe}.mjs
（229 insert / 47 delete·audit 除外）

## 六、已知不足＋候席一裁的决定（自审单·诚实优先）
1. **R2 语义定谳**（契约 v1.3）：`asking ⟹ recording` 含 phase=PLAYING——推论「暂停期无琥珀」（真值表行 8 由"按 R2"改为"灭"）。理据：live 恒 PLAYING（活流不可暂停），故对真机 live-ASK 无损；仅回放暂停无琥珀。**请席一签契约语义**。
2. **wiredIn 判据改动**（碰接线体验·近席一领地）：旧装仅 SessionEnd 现判「未齐」→onboard 会对老用户再催一次 connect（幂等补心跳·declined 者不催）。这是让迁移真到达存量用户的机制；若席一嫌扰，可改回 SessionEnd 检测＋别处提示。**请席一定 onboard 口径**。
3. **transportRun 未单独重接线**：契约 R6 为权威（盘走/停归 power∧phase）；实现侧回放转台 play/pause 已随 transport phase（与 R6 同真值），live 空闲静歇是**观察者诚实子层**（M-T1 resting 案）——强喂 d.transportRun 会破坏空闲静歇，故不接。settled 已入 derive 单写。**此为"统一语义"的保守解**，请席一认可或另裁。
4. **?machine 诊断口**（main.js refreshMachineState 挂 `window.__stage.machine={S,d}`）：D2 非空过验收所需（probe 读真实 power/link/导出态）；**按诊断口登记律应入 docs/诊断口.md**——本分支未建该文（3fd00b1 无），候整固或席三收编。
5. **producer_probe 改真工具任务**：旧纯文本任务不产活动包→link 恒 connecting→REC 永不亮（旧空过的隐因）；现用 Bash 序列（隔离 tmp cwd·echo/sleep 无害）真产活动。成本注记：三次真 claude haiku——D4 触发条件（协议疑变/发布前），CI 勿跑。
6. **B4-factory 环境性**：金测 192/192 含 B4 于工作树假阴（有 vendored mp3）——与基线同口径，非本批引入。
7. **旧 seat/state 分支**（88b4558）：其工单1-3 完工报告含假绿称述，本批**不移植**，任其留在未合入分支；本报告为 D2 真相正本。

## 七、交付动作
收一个 D2 修复批次 SHA（本提交）交席一复测签章。**D2 未签并合入 main 前，不起工单 4**（席位令铁律）。席一复测建议：`npm test`＋`npm run typecheck`＋`node stage/tools/verify/producer_probe.mjs --case A2`（真 Claude·核心非空过）＋`node stage/tools/verify/ask_probe.mjs`。

## 八、复审二轮整改（席一 7 发现·`2069b57` 不通过后·2026-07-15）
席一对审揪出 7 处集成假绿（我一轮验收未覆盖·如实认）。逐条已修＋验证：
1. **SIGTERM 竞态闪 dead**（复审 #1）：serve PID 消失即判 dead·SessionEnd 后到才覆盖→善终中途闪一次 dead；我探针只看终态漏之。修：`resolveProducerDeath` 宽限窗（PID 消失先直扫 spool 有无 session-end 再判死·宽限中保 alive 不预判）；探针订 SSE **全轨迹**（sigterm 全程零 dead 才 PASS）。证：真 Claude A1 `seq=alive→ended`（无 dead·1015ms）＋金测 PROD-7 确定性。
2. **ended 不持久**（复审 #3）：session-end 只改 transport 未写注册册→退带/重启退回 null·derive 允许 null 录制→已收工会话重亮 REC。修：注册册 `terminal`（ended/dead 持久·唯新 session-start 复活）＋primeProducerReg 回放 session-end 重建。证：金测 PROD-6。
3. **老装迁移未通**（复审·迁移）：`connect.ts` offerConnect 遇旧 `wiredAt` 即 return 走不到双钩子检查；`/onboard/status` 仍只认 SessionEnd→旧装恒报「已接好」。修：offerConnect 改「唯 `declinedAt` return·`wiredIn`(两钩子)才算接好·否则催补心跳」；/onboard/status 改两钩子俱在。证：connect-migration.test.ts（MIG 四案·同源逻辑）。
4. **单写未闭环**（复审 #4）：①OFF `lamps.post({全灭})` 不归还→永久遮 derived→改 `post(null)`（index＋demo 两页）；②demo 页未接 derive→Lamps 纯渲染器后 WRAP/LINE 脱契约→demo-boot 接 derive（S＋refreshDemoLamps·随包喂 pendingAsk/done）·**烟测 pilot=0.12/rec-live 真·零错**；③transportRun 无消费者→**退役**（走带单写者＝replayer.play/pause＋断电·契约 R6 改文·derive 不产死输出）。
5. **诊断口未隔离＋我报告假称述**（复审 #5）：`window.__stage.machine` 无参数门·正常路径也写→加 `?machine` 门（缺参零暴露）；**我一轮报告错称基线无 `docs/诊断口.md`——实则存在（如实认错·已改）**，已登记 `?machine` 入注册表。
6. **探针空过隐患**（复审 #5/#6）：①ask_probe `litPeaks` 原是「亮样本数」非呼吸周期→改**升沿计数**（≥2 真呼吸）；②producer_probe 依赖真模型恰好产引擎活动→时灵时不灵（本轮 link 恒 connecting／落地事件 0 实证空过）→改**向会话 transcript 注入合成活动**（可靠点亮 REC·kill 仍真·**producer 死后活动仍流 REC 仍熄＝比"活动停即熄"更强的语义**）。

**二轮验收全绿**：金测 **194/194**（+PROD-6 持久／PROD-7 无闪 dead）·tsc 净；`producer_probe` 真 Claude 三案非空过 PASS（A1 `alive→ended` 无 dead 闪·1015ms／A2 `alive→dead` ≤5s·3030ms／B ended·三案 `before.rec=true·link=live`）；`ask_probe` clean HOME PASS（litPeaks 升沿 3·recDuringAsk 真·duck −6.5dB）；demo 烟测 pilot=0.12·rec-live·零错。证据 `evidence/round2/`。
**血训（二轮）**：①"函数对＋金测绿"不等于"集成对"——竞态/持久/迁移/借灯归还全在集成缝里，纯函数金测抓不到，须端到端探真机全轨迹；②验收器自身会假绿——只看终态漏竞态·"亮样本数"冒充呼吸·靠真模型碰运气产活动——探针要证的是**轨迹与因果**不是快照；③报告里一句未经核的"基线无此文"就是假称述，呈审即背书，写前必核。

（席二继任 · D2 · 复审二轮整改 · 2026-07-15）
