# TAPE-0 施工令 · M1.9 追认与续航（双轨继续）

> 两轨各自继续，围栏与治理沿用 M1.8 §0。协议 schema 冻结不变。

---

## 0. 追认与封版确认

1. **jam.stuckEdges 降级：追认**（判据级理由同构；恒红哨兵违报警疲劳法）。**v0.1.0 封版确认**，三哈希 engine `dd284b1` / params `aac8e0db` / verdict `20af9b64` 存档。verdict/3 签核行有效。
2. SPEC 三横幅追加：确认。
3. 空问题结案：storm 无和弦经真实目标槽复证——`resolveOnOpportunity` 退役由"设计裁决"升格为"实证事实"，白皮书消散教义获实证印章，注记入 verdict。
4. storm 峰值 0.915 贴顶：**不修**。注记入 verdict：近顶峰值是风暴的预期戏剧音区；未来引擎变动引发的换冠军属**例行保养**，按冠军规则机械执行，无需惊动。

## 1. Track-FIX 续航令

### 1.1 v1-live（既定 F5，现开工）
- bounded 广播（禁累积式采样，审计发现 4 教训固化）；**真 20Hz 为正典频率**。
- `replay ≡ live` 逐字节金测试补全（审计发现 7 收尾）。
- 滴灌机制 live 实测（B-5 结案）。
- live 侧 ASK：显式 `AskUserQuestion` 为准；权限等待的探测缺口如实记录，不硬造启发式。

### 1.2 中继修复（舞台手记提请，均批准）
- `curve.csv` 增 `pendingAsk` 列（产物格式，协议不动）。
- `replay` 增 `--hz` 旗标：默认 100ms 存档级；50ms 渲染级按需（内存翻倍自担，用于未来预告片渲染）。

### 1.3 磁带狩猎 v2（真实覆盖归零的补课）
- 猎场：**最新会话，含 M1.8 修复会话本身**（38 金测试红绿循环＝教科书样本）。
- 目标两卷：**真卡碟带**（同 verb+tool+targetHash 单 episode 内 ≥3 败，最好含 ok 型破卡碟）；**释放带**（高张力处 test 转绿或提交收尾）。
- 入册流程照旧：体检表＋引擎判定双件套报批。寻获后：新带接任 jam 角色，`stuckEdges` 以 informational 复活一轮（判据试用期法）再转 active；旧 jam 带转任"轻症"参考或退役；随后重扫＝例行换冠军。
- 一卷都没有 → 记缺照常，缺口保留在 `_batteryCoverage`。

### 1.4 声音相（白皮书全文）
1.1–1.3 交付后进入。里程碑与验收按白皮书 §6；`?tuner=1` 调音抽屉与 sound-params.json 治理同级。

## 2. Track-STAGE · M-S2 令

### 2.1 原令四件
带轴（转速=A、抖=wow、角动量起停）／机械计数轮（悬停微距入画）／镜头法（<2px/s 漂移、动态胶片颗粒 shader 替换静态噪点、暗角动化、暗场抖动）／材质打磨（香槟三色、胡桃木、螺钉细节）。

### 2.2 视觉打磨清单（架构师验帧所出，并入 M-S2）
1. **烟色玻璃**：VU 与走纸表头按材质法罩玻璃，反光克制；顺带解决纸面与 VU 抢亮度。
2. **红区并弧**：红区并入刻度弧本体（印刷在同一条弧上），去浮置。
3. **MAIN 灯绑定**：按灯位法绑为待机粒（通电即暗暖常亮，钨丝奶油色），或撤下——无主器件违器件法。
4. ASK 管可选加玻璃柱暗示（品味自决）。
5. 琥珀亮起时溢光到金属：**保留**，物理诚实。

### 2.3 宪法载入（v1.2）
- 修正案 A（色标＝固有色，光法有权压暗，不得提亮超过高光标）：**批准载入**。
- 修正案 B（绿宝石两义）：**批准**，命名条款：绿宝石只说一个词——**成了**。常亮＝这一场成了（DONE），一闪＝这一件成了（RESOLVE）。
- 新增附注·时间法：**钨丝分频**——倍速回放时走带机构加速，一切"对人说话"的灯与呼吸保持真实时间。机器可以快进，它对人的语速不变。
- 新增附注·介质法：**接带痕**——蒸馏带的时间空洞在纸上留斜痕，不假装播放，不假装不存在。

### 2.4 五带性格照
每带 30 秒定妆屏录（smooth 的平湖、jam 的同槽反复、busy 的高产无雨、silence 的静场、storm 的风暴全景）。**这批素材即未来发布物料的底片**，M-S2 交付时一并。

## 3. 命名轮预告（下一场船长×架构师会议）

v0 已封、脸已存在、素材即将繁殖——改名的成本正在按小时上涨。下一轮由架构师携联网调研主持命名轮：磁带/电台/仪器血统、npm 与域名可用、不撞 TE 的脸。**本轮起两轨冻结"名字敏感面"**：不再新增烧录 TAPE·ZERO 字样的资产（面板丝印保留为占位，性格照构图避开特写机名）。

## 4. 交付清单
- [ ] Track-FIX：v1-live 三件＋中继两修＋狩猎 v2 结果（或记缺）＋（如换带）重扫换冠军
- [ ] Track-STAGE：M-S2 四件＋打磨五条＋宪法 v1.2 载入＋性格照×5＋舞台手记
- [ ] 两轨 FEEDBACK 照旧

（完）
