# TAPE-0 M1.6 交接包 · 读我

一句话：M1.6 主令 + M1.6-A 增补令**全落地、23/23 金测试绿**。扫参 **0/144 全绿** → 按 M1.6-A §5 取帕累托冠军 `886928d1` 作 provisional 参数、已落盘、**照常进 M2**。两处过不了的都是**结构性、非可调**——判据假设被实验证伪，等你裁。

## 读的顺序

1. **01-FEEDBACK.md** — 客观交接：扫参结论、两处 blocker、要你定的 4 件事、本轮现实修正。
2. **02-施工方手记.md** — 我的复盘 + 看到的优化 + 给你的 5 个更大决策点。
3. **03-冰箱登记簿.md** — 缓办项（余温复位/texture/质地分音/分享带/infeasible 标注）。
4. **07-SWEEP_REPORT.md** — 144 组扫参全貌：领奖台、冠军、**敏感度表（参数耦合 H/M/L）**。
5. **04-params.json**（冠军·provisional，`886928d1`）｜**05-verdict.json**（判据即数据，`912ac976`，本件待你签核）｜06-sweep.json（网格）。
6. **曲线-\*/** — 五带各一份 REPORT v3.1 + curve.csv + moments.csv。
7. **探针/** — 五带各一份 `probe.html`（自包含，浏览器打开点▶播放，**用耳朵判**）。

## 要你拍板（4 件，都在 FEEDBACK 二）

1. **storm 的 RESOLVE 判据怎么办**（头号）——迷路型 fetch 风暴**结构上没有高张力处的解脱事件**，全 144 组 storm_resolve 恒 0。改判据 / 换卷 / 认了？我倾向"改判据"。
2. **smooth 99% 卡尺**——这卷顶到 98.95%，网格只更热、永远过不了；是你有意的哨兵。降阈 / 换卷 / 保持报警？
3. **provisional 冠军是否转正**（READ0.21/failDefault0.4 已落盘）。
4. **Tone.js vs Web Audio**——我用原生 Web Audio 替了 Tone.js（CDN 会踩"无网络"硬禁）。认替代 / 要我 vendor 库？

## 大白话：这轮和上轮差在哪

- 上轮 storm 峰值才 0.633、占空 1.5%，怎么调都上不去。这轮**换了把尺子**（雨量 R）+ 扫参 144 组，storm 峰值冲到 **0.903**、占空 **17.9%**——**风暴终于长得像风暴了**。
- 但它**永远发不出"解脱"那口气**（RESOLVE）：这卷是"疯狂翻找型"风暴（78 次网页抓取失败），它的平息是慢慢不找了，不是某一声"搞定"。判据非要它发一声"搞定"，它给不出。
- 探针里**听得见**：storm 的声音只有跳针（卡壳）没有和弦（解脱）；jam 有跳针也有和弦（卡住→挣脱）。**打开 探针/probe-storm.html 和 探针/probe-jam.html 对比听，一耳朵就懂了。**

## heads-up（不用回，告知）

- **出血型风暴（跑挂/写坏为主）全库一卷都没有**——你想要的第六带 storm-b 没找到，我记了缺。可能这个用户的真实风暴就都是"翻找型"。
- **协议 schema 本轮零改动**。verdict.json 是新增治理件，我起草，等你在提交里签核行确认。
- 文件大小：REPORT/FEEDBACK/手记都几 K；**curve.csv 大**（busy ~2MB）、**probe.html 各 ~300K**。要精简就只发 01/02/07 + 各 REPORT.md + 两三个 probe.html。

---
_原始磁带（raw JSONL，含密钥风险）不在此包内、永不外传。curve/moments/probe 均机器生成、无原文。tapes/ 与 runs/ 均 gitignore。_
