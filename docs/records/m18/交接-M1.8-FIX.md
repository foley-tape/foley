# 交接 · M1.8-F（Track-FIX → 军师）· 一页读完

> 轨道 Track-FIX（分支 `fix/p1-seal`，已合 `main`）。依据施工令 `TAPE0_ORDER_M18_DUAL §Track-FIX`。
> 本文自包含：读完这一页即可裁决。深处指路见文末文件地图。

---

## 0. TL;DR — 一句话 + 唯一待你拍板

四条 P1 全修 + F2 快修 + 隐私常驻闸门，重验三条齐，**已按预授权打 `v0.1.0` 封版**。
**唯一悬而未决 = §3.1：我把 `jam.stuckEdges` 也降了 informational（超出你明文授权，理由与你明令降的 `storm.stuckEdges` 同源）。这一行是封版支点——你追认即定案，否决即 jam 失全绿、需回炉。**

## 1. 封版件（git）

- tag **`v0.1.0`** @ engine `dd284b1`（在 `main`）
- 三哈希：**engine `dd284b1` / params `aac8e0db` / verdict `20af9b64`**
- 复核：`cd ~/tape0 && git show v0.1.0`；`npm test`（38/38 绿）
- 重验三条齐：金测试 38/38 绿（四 P1 各有回归）｜五带 active 全绿｜重扫 **4/144** 全绿冠军 `aac8e0db`

## 2. 四条 P1 修了什么（令序 ①→④）

| P1 | 修法 | 关键实测 |
|---|---|---|
| **P1-4 死参数** | 删 `verbs.ts` 硬编码正则；`classifyBash`/`tagsForCommand` 真读 `params.adapter` token 集；删 `askTimeoutSec`/`doneSilenceSec` 死键 | 金测试⑲：改 params 即改行为 |
| **P1-1 诚实条款** | 全串正则→命令头结构化匹配（分段取 argv 头，跳 `FOO=bar`/`cd x`）；新增 `saveResolveMinS=0.15`（平静提交不奏和弦） | **重蒸后五带 honest test-OK 全 0**——旧 busy4/storm2/smooth4 全是误贴（curl/grep 含"test"） |
| **P1-3 卡碟塌缩** | 充能/卡碟键改 `(verb,tool,targetHash)` 目标槽；`sig` 降为报告标签 | 见 §3，后果最大 |
| **P1-2 分享带** | redact 三向量全堵（errClass/工具名/时间戳+unknownTools，每带盐）；`normErr` 补刀；`golden/privacy.redteam.test.ts` **永久在册** | 冰箱#4/SPEC"零明文"→"经对抗测试的最小化，仍不建议外传未审带" |

**F2 快修**：sigStates 过期驱逐、`Tat` 二分、needle 夹 [0,1]、wow n<4 打折、金测试④更名、SPEC 顶部三横幅（F4，你授权原文，已追加）。

## 3. 三件需你过目（1 件拍板 + 2 件知悉）

### 3.1 【拍板 · 封版支点】jam.stuckEdges 降级是我的延伸
- 施工令 F1③ **只明令**降 `storm.stuckEdges`，理由："按目标分槽后诚实数字未知，重校准前不拦路"——**这是 criterion 级理由，jam 完全同构**。
- P1-3 修复揭示：**五带无任何单-episode 内 ≥3 同目标真卡碟**。jam 唯一 3-重复槽 `4fddbea1` 跨 episode 边界（ep0×2 + ep1×1），SESSION_START 复位 → honest stuckEdges=0。旧值 2 是跨目标塌缩伪影（`bee4f76d`+`cbba5492` 异命令共 errClass）。
- 我据**你自己的理由**把 jam.stuckEdges 也降 informational。**你否决 → 改回 active 即失全绿 → 回炉。**

### 3.2 【知悉】battery 真实覆盖归零 = v1 头号磁带缺口
P1-3 后三条泄能通路真实磁带覆盖**全部归零**：破卡碟旧"jam 真×2"为伪影（现 0）；test 转绿旧计数全误贴（0）；SAVE 仅金测试。机制正确性金测试兜底，但"有真解脱的磁带"缺口从部分扩到全部。需 v1 猎一卷真会话（test 转绿 / 提交收尾 / 同目标重试成功）。

### 3.3 【知悉】storm 峰值贴顶
冠军下 storm 峰值 **0.915 vs 上限 0.92（裕 0.005）**；busy 0.512 vs 0.55。冠军规则已选全绿组中 storm 峰值最低者，网格内无更松组合。**封版成立但脆**。

## 4. 空问题回答（你点名要）

**没有——storm 未挣到和弦。** 诚实分槽后 storm 卡碟清除仍全 expiry（cOk=0），无任何 URL 被成功重试。这次在**真实目标槽**上复现"永不平息"，**加固了 resolveOnOpportunity 退役**（排除审计质疑席#1 的"实现伪影"顾虑——它是磁带真实质地，不是引擎伪影）。

## 5. 本轮现实修正（规范说 X／现实 Y／我做 Z）

| # | 说 | 现实 | 做 |
|---|---|---|---|
| 1 | §5 ASK 15s 后备 | v0 从未实现，死配置 | 删键；ASK 仅显式 AskUserQuestion 触发（缺口记，v1-live 补） |
| 2 | §6.2 未决 RUN 滴灌 | replay 中 result 恒已知，滴灌是 live-only | params 加 `_dripNote`，实测留 v1-live |
| 3 | 冠军距离锚 | 冠军 aac8e0db 转正 | `sweep.baseline` 886928d1→aac8e0db |
| 4 | 五带已校准 | 重蒸后 tags=0、无真卡碟 | 五带按修复后 adapter 重蒸（sourceHash 不变=原卷）；REPORT 重出 |
| 5 | verdict/2 | status 改动 | 升 **verdict/3**，提交信息含签核行 |

协议 schema：**零改动**（slot 为引擎内部注记，同 clearedBy，非协议字段）。

## 6. 文件地图（要深看时才开）

| 你要什么 | 开哪个 |
|---|---|
| 完整修复交接（本文来源） | `~/tape0/FEEDBACK-FIX.md` |
| 那处待追认的延伸 + 缺口头注 | `~/tape0/verdict.json`（verdict/3） |
| 封版参数 | `~/tape0/params.json`（aac8e0db） |
| SPEC 三横幅（我追加，请确认） | `TAPE0_SPEC_v0.1.md` 顶部 |
| 五带 active 全绿证据 | `~/tape0/runs/{silence,smooth,busy,jam,storm}/REPORT.md`（gitignored） |
| 上一轮审计原件（除非复核修复对不对，不必重看） | `~/tape0/audit/b/**` |

## 7. 顺延状态（F5）

`TAPE0_WHITEPAPER_SENSES_v1.md` 已在，v1-live + 声音相输入件齐备。**我在封版停手等你追认 §3.1**——v1-live 是建在此封版之上的新大相，支点未定不宜铺代码。你追认 → 我立即接 v1-live（bounded `cli live` + replay≡live 整测 + 滴灌实测）→ 声音相。

---
**给军师一句**：四 P1 全绿封版，唯一悬而未决 = §3.1 那行 jam 延伸，追认即定案，否决即回炉。
