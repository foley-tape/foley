# 示范带发布前置审计（M2.5 §B.1）

> 对象：`stage/fixtures/storm.{curve,moments}.csv`（demo 站内置示范带；随 npm tarball 同一份）。
> 判：**过审入页**。审计时刻 2026-07-06；执行=Track-STAGE；方法与产出如下，可复跑。

## 一、NIGHT 隐私回归（redact 级复核的机器形）

`node --test golden/privacy.redteam.test.ts` → **5/5 全绿**：
七类密钥无明文子串存活；MCP 工具名（公司/项目/动作）无明文；绝对时间戳/日历指纹去除；
内联凭据/含数字令牌/URL 抹 SECRET；邮箱 PII 抹 EMAIL（NIGHT-2 A1）。

## 二、列级审计（零自由文本红线）

- **curve.csv（39k 行）**：九列全过——t/S/T/A/wow/needle 数值；phase ∈ {IDLE,WORKING,WAITING,DONE}；
  weather ∈ {CLEAR,OVERCAST,RAIN,STORM}；pendingAsk ∈ {0,1}。无任何自由文本位。
- **moments.csv**：verb ∈ 七动词枚举；outcome ∈ {OK,FAIL,NA}；special ∈ 六特情枚举；
  sig/k/slot 全 ≤16 位 hex；clearedBy ∈ {ok,expiry,∅}；tags 仅 `[\w,|-]`；m 数值。
  **会话文本、路径、代码一字不存**（蒸馏架构的构造性保证，此处逐行复核）。

## 三、指纹（与 demo 站构建纪要对账）

```
curve   sha256/16 = d6cbad0710b35377
moments 见 DEMO-NOTES（构建时同源复算）
带      = storm（真实会话蒸馏带，M2.1 §0.7 封版 CLI 只读重放产物）
```

## 四、橱窗纪律核对（demo 站）

无 serve／无写盘端点／无导出件（vendor 不随站）；唯一交互=POWER；
首屏横幅 "This is a recording — run `npx foley` to hear your own."；
唱片署名页脚（Still Life · HoliznaCC0，人类制造·CC0，血统条款）。
