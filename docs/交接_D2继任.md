# 交接 · D2 继任（席二·四轮拒签后）

交棒：席二继任终端（上下文将尽）· 2026-07-16 ｜ 接棒：D2 继任
**开工先读**：①本文 ②`audit/seat2-state-d2/复盘_D2反复不过_根因分析.md`（血训·别重蹈）③`audit/seat2-state-d2/D2_完工报告.md`（一/二轮细节）④`FOLEY_LEDGER.md` 工作法。

## 〇、第一件事（最重要）
**别急着动码。先向席一/船长要到"第四轮拒签的具体发现"**——本终端交接时**未收到本轮具体 P0**，只知"又出几个 P0·第四次"。**没有对手方的验收标准就动码＝第五轮必挂**（复盘 §五.1：对手方先写可执行验收单，你再使其变绿）。若能，请席一把它的对抗检查写成可执行断言给你。

## 一、工地实况
- worktree `/Users/shadow/tape0-seat-d2`，分支 `seat/state-d2`，**基线 `3fd00b1`（席一 WO1/2 已在 main）**，其上**一个提交** `d9413d6`（D2 全批·三轮迭代折成一提交）。树净·未推。
- node_modules 双软链已配（→ `/Users/shadow/tape0/node_modules` 与 `.../audit/tools/node_modules`）；worktree 局部 exclude 已写（`git rev-parse --git-path info/exclude`）。
- SHA 谱系：一轮 `2069b57`（驳）→ 二轮 `405a624`（驳）→ 三轮 `d9413d6`（本轮·又驳）。都用 `git commit --amend` 折成单提交，故只见一个 SHA。

## 二、已修（rounds 1-3·勿重开，除非本轮 P0 指向它）
- **契约 v1.3**：`docs/状态契约_模式灯语真值表.md`。R2 `asking ⟹ recording`（清零非 live/ended/未录问询）；settled 入 derive（done 输入·R5）；transportRun 退役（无消费者）。
- **derive 单写**：`stage/js/derive.js`＋`stage/js/main.js` refreshMachineState（rec/ask/signal/LINE/settled 唯一写者）；`Lamps` 退纯渲染器（`stage/js/instruments.js` onPacket 只留魔眼活动强度）。
- **serve producer 心跳**：`stage/serve.mjs`——producerReg（含 `terminal` 持久＋`sessionId`）、`producerGen` **代际**（arm/disarm/当前会话 ended +1·旧 timer/resolver/session-end 认代际非当前即弃）、`resolveProducerDeath` 宽限窗（SIGTERM 不闪 dead）、`spoolHasSessionEnd/markSessionEnded` 按 sessionId 门、`GET /transport`、`/onboard/status` 两钩子。
- **connect 迁移**：`cli/connect.ts` wireSettings 双钩子（SessionEnd＋SessionStart）＋wiredIn 判"两钩子俱在"＋offerConnect 不再单凭 wiredAt 短路；`cli/hook.ts` SessionStart PID 报到。
- **demo 不伪装 live**：`stage/js/demo-boot.js` `sourceKind:'session'`→REC 不亮·接 derive。
- **OFF 闭环**：`stage/js/post.js` runPost frame 查 `opts.abort`；`main.js`＋`demo-boot.js` `postRunId` 代际中止在跑 POST（OFF +1）。
- **诊断口**：`?machine`（main.js·`params.has('machine')` 门）→ `window.__stage.machine={S,d}`；已登记 `docs/诊断口.md`。

## 三、验收工装（怎么跑）
- 金测：`npm test`（195/195·含 **PROD-6 持久／PROD-7 无闪 dead／PROD-8 代际隔离**·`golden/producer.test.ts`）＋`npm run typecheck`（净）。
- **producer_probe**（`stage/tools/verify/producer_probe.mjs`·**已改全合成·不碰真 Claude/~.claude**·victim PID＋合成 transcript＋隔离 tmp）：`node ... --case all`（kill：alive→dead≤5s＋死相；ended：alive→ended 全程零 dead）。全轨迹＝SSE producer 全序＋rec 快采（recWasLit→recAfter）。
- **ask_probe**（`.../ask_probe.mjs`）：clean HOME（FOLEY_HOME=tmp）·litPeaks=升沿计数·recDuringAsk·duck。
- demo 烟测脚本在 scratchpad（会被清扫·脚本可重写）：开 `demo.html?autoplay=1`+通电手势→查 `recLive===false`＋pilot>0＋零错。
- ⚠️ **验收器等待条件律**：不赌固定睡眠，喂到阈值或诚实超时。⚠️ **换码必清 profile/重启 serve**（Chrome 吃缓存旧页假验收，账本发现栏在案）。

## 四、四轮反复的教训（照做，别重演·详见复盘）
1. **自测全绿≠过**——账本铁律，我四轮栽在这。交付前**主动用对抗场景打自己**：新会话在死亡中途插？OFF 撞 POST？退带撞判死？重启后 ended 会否退 null？
2. **真 bug 在时序/并发轴**（竞态/代际/生命周期/持久/隔离），不在 derive 快照轴。任何有状态改动，过一遍"并发自审清单"。
3. **验证器要比被测代码简单**，否则它自己就是 bug 源（producer_probe 我改了四版才不撒谎）。
4. **代际（generation/epoch）是这类"长活监听短活"的头号不变式**——serve 早有 `transport.epoch`、live 有纪元自愈，producer 却拖到三轮才补。设计时就该建。
5. **一批次一上下文**：拒签就换新终端续做（本轮拖成 marathon＝错误率升）。

## 五、若本轮 P0 指向新面
- 先定位到 §二 哪一块，读那块＋对应金测/探针，**先加一条能复现该 P0 的断言（红）**，再改到绿——test-first，别改完再补测。
- 涉 serve producer：`producerGen`/`terminal`/`sessionId` 三者交织是雷区，改前把"状态×事件×代际"在纸上画全再动。
- 涉 OFF/POST：`postRunId` 在 main.js 与 demo-boot.js 各一份，别漏一页。
- 涉探针：保持全合成隔离，别回头用真 Claude（复盘 §三.D 血证）。

（席二继任 · 四轮后交棒 · 2026-07-16 · 球在"要到本轮具体 P0＋对手方验收单"）
