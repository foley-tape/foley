# 交接 · 席三「门禁与法典」· 给下一个新终端

> 交棒：席三首轮（本终端）· 2026-07-15 · 上下文将满，按 D5「一批次一上下文」收口交棒。

## 0. 你是谁 · 开工三读
- **席三 = 让规矩自动执行的机器**：CI 门禁／回归资产／治理文档／正典编纂。你**同时是席一的审计员**。
- **不碰**：README 文案本体（席一）、页面状态机（席二）、一切品味项（冻结）。
- **工地**：worktree `/Users/shadow/tape0-seat-gates` 分支 `seat/gates`（基 `main=6766d93`·**未推未合**）。node_modules 与 audit/tools/node_modules 软链自主仓（右耳 APPENDIX 法）。
- **开工三读**：仓根 `FOLEY_LEDGER.md` → 本交接 → `memory/seat3-gates-charter.md`。原始席位令 `~/Downloads/FOLEY_SEAT3_GATES.md`＋接力裁决 `~/Downloads/FOLEY_RULING_SEAT2_RELAY.md`。
- **开工先查并发**：`git worktree list`＋各 `seat/*` branch diff（当前席一 seat/trust·席二继任新终端）。

## 1. 已交（seat/gates 11 commit·树净·未推未合）
| 工单 | 交付 | commit |
|---|---|---|
| item 1 账本回写 | 夜审归档 text-only／门禁台账§二·七＋发现栏 NIGHT-3 块 24 条 | 06d1f61·daddd79 |
| item 2 回归族 4/4 | ①transport 假红修复 ②latecomer 密闭化+浏览器闸入默认 npm test ③selector/counter 提纯律金测 ④verify 箱清单 | 4fa403a·4d9b545·d246a2b·1847f93 |
| item 2 诚约族 3/4 | 包体预算闸(严格 2,000,000 B)／账本回写检查／README 对表闸 | a3f7fc3·6b46b56·e73103e·30ccd51 |
| 席一工单 1/2 对审 | **正式 PASS 入账**（船长批） | c8a000f·6b46b56 |

- **金测 189/189·tsc 绿**。三诚约闸皆入 `prepublishOnly`，**不入默认 npm test**（现红属席一 seat/trust 未合·合入即转绿）。
- 对审报告：`audit/seat1-review/ITEM_1-2_REVIEW.md`（含「签名旧债」节）。

### 继任续交（2026-07-15·本终端·基 main=3fd00b1·seat/gates +5 提交·金测 189→231·tsc 绿·船长令「完成可即推·遇界即停」）
扩编批「可即推」逐条落地（详 `FOLEY_LEDGER.md §二·七续`）：
| 项 | 交付 | commit |
|---|---|---|
| 回归族续 | transport 相位穷举 18 例（hermetic serve 黑盒全格转移·CUEING×select=拒钉现状）＋spool 轮转健康 6 例 | 916646a |
| 性能族 | 卡拍编舞值班律 10 例（借 ReelDeck 单源 thetaAt·挣扎—歇 refute 节拍器）·CPU 真表＝真机档未做 | 9786712 |
| 物理族 | 板坐标几何自洽 8 例（维度/不逃逸/旋钮⊂座环/**相机契约不漂移一像素**） | 9786712 |
| item3 治理真话（可先动面） | 冰箱#4 反转结案重写＋GATE 去 302＋NIGHT-3 超覆盖旗 | e3f9bdf |
| 治理三小刀 | 法12/13（船长批）＋勘误录+5＋验收器 D4 触发档标注 | 140c864 |

- **五提交皆本地 seat/gates·未推未合**（合入归操作员/船长）。
- **遇界即停（未动）**：README「never stored」正文＝席一；签名绑哈希＝等席一 item6；状态族/derive/honesty3＝等 seat/state 合入；serve CUEING「排队而非拒」产品处方＝席二状态机域。
- 只测不改他席码：transport/spool 黑盒驱 serve、性能族借 deck.js 原型——皆测/改分离，零改 `serve.mjs`/`deck.js`。

## 2. 你的第一件事 = 新法四条抄入 LEDGER §一 工作法（接力裁决§二）
1. **交付即合并（批次粒度）**：绿测＋对审签章即合·**未合入 main＝未完成**。
2. **等待声明制**：受阻必在账本**等待栏**写一行「席 X 等 席 Y 的〈具体物〉以〈动作〉」·无声明之等待＝空转。
3. **验收分档制**：P0＝验收器＋金测＋真机；P1＝金测＋单跑真机；P2＝金测。**档位签发人派单盖定·不由执行者临场自裁**；贵验收器（真 Claude／真耳）标触发条件（版本升级／协议疑变／发布前）·永不入例行回归。
4. **品味收敛法**：人眼人耳判据**禁逐版盲试**·参数矩阵一炉多宫＋圈选。
（D5：一批次一上下文·领批前查余量·批收口四件套＝报告＋账本回写＋提交＋交接锚·不跨批次续命。）

## 3. 扩编批 · 你的活（接力裁决§三）
1. **新法四条抄入账本**（首件·上）。
2. **transport phase 穷举转移测试**：`stage/serve.mjs` transportSelect/Play/Pause/Eject 全 phase（EMPTY/CUEING/PLAYING/PAUSED）穷举转移金测（纯 node·serve 逻辑面）。**可即推**（serve 已在 main）。⚠见勘误①CUEING 竞态。
3. **spool 轮转健康检查**：spool 事件轮转健康检查。**可即推**。
4. **验收器「何时跑」标注**（D4 附属）：`stage/tools/verify/{still,post_probe,record}`＋`golden/browser-wiring.test.ts`＋audit repro 逐一标触发条件（例行/发布前/协议变）。扩现有 `stage/tools/verify/README.md`。
5. ~~p0-2 transport.mjs 假红~~ **已修（回归族①·4fa403a）**——扩编批此项已闭。
- 登记表其余：cli ENOENT／gone 粘滞＝**席二继任 工单4**；transportRun 执法＋Lamps 双喂＝继任后续批；doctor 心跳行＝任一席顺手小刀（非你独占）。

## 4. 等待栏（我已声明的 waits·抄入账本等待栏）
- ~~**席三 等 席一的〈item6 签名绑哈希规则正文〉以〈逐字机器化签名闸·诚约族补 4/4〉**~~ **✅解锁 2026-07-16（整合批）**：item6 发布 `docs/canon/SIGNATURE-HASH-CONTRACT.md` v1→逐字机器化 `scripts/check-signature-hashes.mjs`+8 自证+**B8 当前切片重签绿**·诚约族 4/4 齐。
- ~~**席三 等 seat/state 合入 main 的〈deriveMachineState 纯函数＋状态契约〉以〈状态族·honesty3〉**~~ **✅解锁 2026-07-16（整合批）**：seat/state D2 合入 main（`573f370`·契约 v1.3·derive.test.ts 3072 穷举）→席三补 `rec-honesty-guard.test.ts`（REC 单一写者结构面·引用不重复）。
- **两 waits 皆解·等待栏现空**。整合分支 `integ/gates-on-main`（基 main=`efffb94`·未推未合），详账本 §二·七续二。

## 5. 可即推 vs 等（决定你怎么排批）
- **可即推（不被 waits 阻）**：新法抄账本／transport phase 穷举／spool 健康／验收器标注／**性能族**（风暴带值班金测；整机 CPU<5% 真表候真机·标 P0 触发档）／**物理族**（几何断言清点·建构期 assert）／**item3 治理真话**（GATE.md 销「never stored」旧绿灯账＋`--raw` errClass 字段／冰箱#4 反转重写——READMEの真随席一合入更稳，但账本/GATE 面可先动）。
- **等**：状态族/derive 穷举/honesty3（等 seat/state 合入）·签名绑哈希（等席一 item6）。**两 waits 皆非启用前置**——边推可推项边候解锁。

## 6. 剩余工单地图
item2 余：状态族✅（整合批 689cc48 REC 单一写者源守·引用席二 derive.test.ts）／性能族✅（继任交 9786712·CPU 真表待真机）／物理族✅（继任交 9786712）／**诚约族 4/4✅**（整合批 6bcd4f4+7d0d461 签名闸+B8 重签）。**item3 治理真话**◐（ledger/GATE 面已交·继任 e3f9bdf；**余**：README「never stored」正文归席一＋工单4 修宪）。**item4 正典编纂**（正菜·三新修宪：档案域数字条款／操作卡条款／蓝光修宪案[照左耳§7·氩蓝 CUE 待七人远距盲测]）。**item5 编纂交付**→席二对审→船长→入仓替位·旧手令入 docs/archive/。

## 7. ⚠ 勘误/坑（血泪·省你的命）
1. **serve live 自装 CUEING 竞态**：deep-link `?tape=X` select 撞 serve 启动 live 自装→`transportSelect` 于 CUEING 期返 **400**。修法＝**空 `FOLEY_PROJECTS` 密闭化**（无 live 会话→无竞态）＋**重载重试**。影响任何 deep-link mount（hero 截图/demo/测试）。
2. **git core.quotePath 转义 CJK 文件名**：`ls-files` 输出 CJK 路径被八进制转义→须 `ls-files -z`（check-ledger-writeback 踩过）。
3. **浏览器闸不脏树**：browser repro 在 npm test 里跑会写 1.2MB 截图→`FOLEY_GATE` 环境守卫免落工件（latecomer 已装）。
4. **红态闸不入默认 npm test**：现红属席外（席一）·入 prepublishOnly·勿破他席开发绿。
5. **验收单位显式 bytes**：预算 **2,000,000 bytes**（十进制）≠ 2 MiB(2,097,152)。
6. **MODE=still≠板相机／headless rAF 须 ?autoplay 自泵／合成手势走 page.mouse 不发 pointerdown**——详 memory[[foley-project-state]]/[[viewer-integration]]。

## 8. 器具速查
- 诚约闸：`scripts/check-{pack-budget,ledger-writeback,readme-contract}.mjs`（+ `check:pack/writeback/readme` npm script·皆入 prepublishOnly）。`--root` 可指别处工作树（对审用）。
- 回归闸：`golden/browser-wiring.test.ts`（latecomer·合成夹具 `golden/fixtures/latecomer.session.jsonl`）／`golden/{selector,counter}-law.test.ts`／提纯律 `stage/js/{selector,counter}-law.js`。
- 验收器：`stage/tools/verify/`（README 立箱）。
