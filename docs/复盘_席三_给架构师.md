# 汇总 · 给架构师 · 席三「门禁与法典」首轮

> 席三首轮 · 2026-07-15 · 上下文将满收口。配套：`docs/交接_席三_门禁与法典.md`（继任蓝图）。

## 一、交付摘要（seat/gates 11 commit·未推未合·金测 189/189·tsc 绿）
- **item 1 账本回写**：两耳夜审归档 text-only＋LEDGER §二·七门禁台账＋发现栏 NIGHT-3 块 24 条（右 D-1..14／左 #1..10＋接缝／渲染批挂账八条·全覆盖）。
- **item 2 回归族 4/4**：transport 假红修复／latecomer 密闭化＋浏览器闸入默认 npm test／selector·counter 提纯律金测（+14 例）／verify 箱清单。
- **item 2 诚约族 3/4**：包体预算闸（严格 2,000,000 B）／账本回写检查／README 对表闸——三闸入 prepublishOnly。
- **席一工单 1/2 正式对审 PASS**（诚约闸复测＋独立复跑其码测）。

## 二、我看到的、你和船长都没提但值得做的（本汇总重点）
1. **夜审「机制」诊断需复验，不只症状**：transport 假红右耳判「spy 钩旧 `sound.pause` 名」——实证真因是**旧驱动序列高板重排后崩＋serve 启动 CUEING 竞态**，机制归因不全（症状 wired:false 真）。建议成法：夜审的**机制归因入账标「待复验」**，施工者以实证定谳（"实证快于理论"是本轮最省时的一条）。
2. **serve live 自装 CUEING 竞态 ＝ 产品级脆弱（非仅测试）**：deep-link `?tape=X` select 撞 serve 启动 live 自装→`transportSelect` 于 CUEING 期返 400。影响 **hero 截图/demo/任何 deep-link mount**，只是无 live 会话时侥幸不发。建议：serve select 于 CUEING 期**排队**而非拒（或 deep-link 内建重试）。
3. **红态闸的归属与放置应成法**：README 对表/包体预算现红属**席一**；若入默认 npm test 即破他席绿。我的处置＝入 prepublishOnly、default test 只放 green-able 自持闸。建议与 D4 验收分档制并条：**跨席红态/release 闸一律 CI 档·不入例行 npm test**。
4. **签名绑哈希制度的首个活证已现（实况非假设）**：席一降采样 captain.curve.csv（13MB→152K）**当场令 B8 旧签失效**（旧签按文件名+大小·无摘要）。建议席一 item6 规则含**「主体文件 hash-mismatch 即签失效」检测**，不止「新签绑 hash」。
5. **两耳隐私发现非冲突而是两域**：右耳六向量绿（出屋 distill/dub）＋左耳 P0（本地 rack.json 存 80 字开场白）＝不同数据路径。这正是 **item4 档案域数字条款**的实证底。建议修宪明文 **面板域／出屋域／本地档案域 三分**（非两分），席一 PRIVACY-CLAIMS-MATRIX 已隐含此结构。
6. **提纯律可再扩**：selector/counter 提纯（seal-law/vu-law 先例）零回归、tsc 稳。tower 缓动/pack 演进/VU 三点复签等页面纯逻辑可同法金测化——**但须与席二状态机边界对齐**（本轮 selector/counter 与席二无交叠·安全）。
7. **工具坑成册**：git core.quotePath CJK 转义（须 ls-files -z）／浏览器闸脏树（FOLEY_GATE 守卫）／验收单位 bytes vs MiB 二义。建议汇入 `docs/工程勘误录`（现散在提交信息里）。

## 三、等待 vs 可推进（决定继任启用时机）
**结论：继任可即刻启用**——可推进工作充足，不被 waits 阻塞。
- **可即推**：新法抄账本／transport phase 穷举／spool 健康检查／验收器标注／性能族（风暴带值班金测）／物理族（几何断言）／item3 治理真话（GATE.md 销旧账/冰箱#4）。
- **已声明 waits（两条·皆非启用前置）**：①**签名绑哈希 等 席一 item6 规则正文**；②**状态族/derive 穷举/honesty3 等 seat/state 合入 main**（deriveMachineState＋契约 v1.2·接力裁决§四已排解锁次序）。继任边推可推项边候解锁。

## 四、诚实边界
- 对审只到 seat/trust **未提交态快照**；席一提交若微调措辞，`check-readme-contract` 正则不吃行号、自动复验。
- 三诚约闸现红属预期（席一合入即绿），非漏。
- **上下文经济**：本轮大头耗在 transport 假红深挖（browser repro 反复迭代）——若早有「空 FOLEY_PROJECTS＋重载重试」模板可省近半。建议此模板入 verify 器具箱供继任复用。

— 席三 · 2026-07-15
