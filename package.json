{
  "name": "tape0",
  "version": "0.0.0",
  "private": true,
  "description": "纯本地、零遥测。把一场 coding agent 会话翻译成一台仪器的实时状态。",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "scan": "node cli/index.ts scan",
    "replay": "node cli/index.ts replay",
    "live": "node cli/index.ts live",
    "probe": "node cli/index.ts probe",
    "typecheck": "tsc --noEmit",
    "test": "node --test \"golden/**/*.test.ts\""
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
