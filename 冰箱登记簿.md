# TAPE-0 冰箱登记簿

> 已认知、经架构师裁为**缓办**的项。登记＝不遗忘、不擅动。每项含：来源裁决 / 状态 / 解冻条件 / 草图。

## 1. episode 软复位（跨段余温）
- **来源**：M1.6-A §1.二.5（缓办入冰箱）。
- **现状**：v0 硬复位维持——SESSION_START 把 S/rep/卡碟/wow/天气/针全部归零。"一坐＝一个叙事单元"是有意的建模选择。
- **草图**：跨段携带 `S' = min(0.15, 0.2 × S_end)`，让 resume 一场苦战时留一点情绪残温。
- **解冻条件**：确认"多日续跑的情绪连续性"值得建模（v1 舞台期）。

## 2. StatePacket `texture` 字段（风暴质地）
- **来源**：M1.6-A §1.二.3（入冰箱，v1 裁）。
- **现状**：协议 schema 冻结、本轮零改动。texture 不入 StatePacket。
- **草图**：texture ∈ {wander(迷路)/bleed(出血)}，由失败动词分布派生。**注**：本轮扫库证实全库风暴皆迷路型、出血型零样本——texture 现在只有一个取值，建模价值待出血样本出现。
- **解冻条件**：出现出血型风暴样本 + 协议解冻签核。

## 3. 质地分音（迷路/出血不同声音）
- **来源**：M1.6-A §1.二.3 + 手记二.3。
- **现状**：不设——v0 三音（拨弦/和弦/跳针）不分质地；且"不设第四音"禁令在。
- **草图**：迷路型跳针 vs 出血型跳针用不同音色/包络区分（仍算"跳针"一音的两种质地，非第四音）。
- **解冻条件**：#2 texture 落地后，v1 声音层。

## 4. 全脱敏分享带（默认可分享形态）—— ✅ 结案·现状已反转（NIGHT-3 治理真话 2026-07-15·席三工单3）
- **来源**：M1.6-A §1.二.4。
- **反转纪要**：本项原记「**默认不脱敏 + v0 禁导出**」；NIGHT-3 双盲（左耳 P2·`audit/night3-L` 报告）实证代码/README 已反转，据现状结案：
  - **默认即脱敏**：M2.6 G7「默认形态即安全形态」——`distill` 默认 `redact=true`（时间相对化＋非内建工具加盐哈希＋errClass/sig/targetHash 重盐＋sourceHash=redacted），「不脱敏」翻转为显式 `--raw`。金测 `golden/g7.redaction.test.ts` 执法。
  - **导出机制已在**：dub `meta.json` 抹钟（createdAt/liveEpoch）＋mp4 `mvhd/tkhd/mdhd` 钉 0（`stage/js/mp4scrub.js`）——本地导出脱敏管线成形。
- **诚实边界**（NIGHT-1 B 证伪旧「零明文」字样，已并入 G7 修）：`--raw` 原始带含绝对时间＋明文工具名＋精确 sourceHash（stderr 已强警示）；脱敏口径一律以 g7 金测为准，勿据旧「零明文」外传 `--raw` 带。
- **不复活**：**托管/在线分享**仍不做（v0 边界）——只保留「经确认的本地分享物」意图，不复活一键托管导出。
- **残余解冻条件**：v2 托管分享里程碑 + 分享功能解禁（届时才谈在线分享·非本地脱敏，本地脱敏已结案）。

## 5. 验收判据 `infeasible` 标注 —— ✅ 结案·已迁出（M1.7）
- **来源**：M1.6 手记三.3（施工方提）。
- **归宿**：M1.7 §1.1 采纳并推广为**判据生命周期 status 三态**（informational/active/**retired**）。storm.resolveOnOpportunity 已 `retired`（范畴错误），扫参器跳过计分仍记实测值。infeasible 的诉求由 status 机制完整覆盖，**本项结案，不再占用冰箱**。
- （保留此条仅作索引；机制已入 verdict/2 + 金测试 ⑯。）

## 6. 唱片层视觉具象化（面板上的"唱片在转"）
- **来源**：SOUND-R3 施工令 §1（架构师裁：唱片层音频先行，**面板零新器件**，视觉具象化入冰箱候裁）。
- **现状**：R3 已交付音频面（probe HUD 文本行+⏭ 换盘，dev 设施不算面板器件）；stage 琥珀面板零改动。
- **草图**：走带轮→唱片盘的视觉双关；换曲=换面动画；tape-stop 滑停与带轴惯性滑停同帧（宪法"动法"惯性条款现成）。器件法两义封顶约束下可能只配一个变量：唱片在位/滑停。
- **解冻条件**：船长实听过耳（R3 身份终裁成立）＋架构师裁视觉轮开工。

## 7. 天气选曲（weather → 唱片类目）
- **来源**：SOUND-R3 施工令 §1（catalog 天气类目记录在案，**天气选曲留 v1.x**，本轮不做）。
- **现状**：open-lofi seasonal-weather 类目 27 首在案（sound/records/catalog.json `_weatherCatalogOnRecord`）；weather 档位处置差异亦未做（白皮书 §2.0b 标未执法）。
- **草图**：RAIN/STORM 天气锁存（wxLatch 既有）→ 下一小节边界换对应类目唱片；或同唱片处置档位化（更轻）。
- **解冻条件**：v1.x＋美学轮对"处置档位 vs 换盘"二选一有裁决。

## 8. 实录参考锚（盲选包的第五页）
- **来源**：SOUND-R3 现实修正 R3-2（主单要"lo-fi 房间实录"锚；本轮用唱片直通=同素材消融锚替代，候裁）。
- **现状**：runs/aesthetic-pack/ 四页制（锚=唱片直通+A/B/C）；README 已注记偏差。
- **草图**：若裁需真实录——vendor 一段 CC0 房间实录音乐（Freesound，授权工序照 records 纪律），作"绝对口味锚"与消融锚并列第五页。
- **解冻条件**：船长盲选第一轮结果显示需要外部口味参照（消融锚不足以定夺）。
